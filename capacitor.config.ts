import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-tabs-template',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
